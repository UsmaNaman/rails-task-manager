<h1>📝 - My Task! - 📝</h1>

<h2>Title: <%= @task.title %></h2>
<p><strong>Details</strong>: <%= @task.details %></p>

<%= link_to "Task List", tasks_path %>
<%= link_to "Edit", edit_task_path(@task), method: :patch %>
<%= link_to "🗑",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
